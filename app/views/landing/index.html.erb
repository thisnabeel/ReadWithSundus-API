<div class=" flex justify-center items-center">
  <div class="p-6 w-full max-w-4xl">
    <div class="flex justify-between items-center mb-4 w-full">
      <span class="bg-lime-200 text-gray-700 rounded-full px-4 py-1 text-sm w-full text-center">Accepting New Enrollments for 2025</span>
    </div>
    <div class="flex flex-col md:flex-row items-center md:items-start md:space-x-6">
      <div class="flex-1 md:flex-grow-0 mt-16">
        <%# <p class="text-gray-500 text-sm mb-2">8 dx 30 min. </p> %>
        <h1 class="text-5xl font-bold text-gray-800 leading-tight">Read<br>With<br>Sundus</h1>
        <a href="/enrollment_requests/new" class="inline-block mt-4 bg-purple-200 text-purple-700 rounded-full px-3 py-1 text-xl">Apply Now ★</a href="/enrollment_requests/new">
      </div>
      <div class="flex-1 md:flex-grow">
        <!-- Placeholder for illustration -->
        <img src="<%= asset_path('cute.gif') %>" alt="Cute characters" class="w-full h-auto">
      </div>
    </div>
    <div class="mt-12">
      <h1 class="text-4xl font-bold text-center my-5">Grades:</h1>
      <%= turbo_frame_tag "grades-wrapper" do %>
        <%= render partial: "tabs", locals: { active_tab: @active_tab } %>
        <%= render partial: "grades_content", locals: { active_tab: @active_tab } %>
      <% end %>
    </div>
  </div>
</div>
